fastapi
uvicorn
python-multipart
psycopg2-binary
Pillow>=10
# AI Safety Agent (LangChain ReAct + Gemini). The app runs without these
# installed — /api/agent simply falls back to the scripted assistant.
langchain-core>=0.3
langchain>=0.3
langgraph>=0.2
# Require a release that serializes thinking_level for Gemini 3.6: older 4.x
# can silently drop it for models missing from their capability registry.
# The 4.x retry setting counts attempts (1 = no retries).
langchain-google-genai>=4.4.0,<5.0
